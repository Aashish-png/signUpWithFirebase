rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /students/{documents=**}{
    	allow read: if true
    	allow update: if true
    	allow write: if true
    }
  }
}